{
  "name": "haider",
  "version": "0.0.1",
  "scripts": {
    "start": "npm run build && npm run serve",
    "watch": "npm-run-all build -p serve-watch build-watch",
    "test": "jest",
    "build": "tsc",
    "serve": "node dist/server",
    "build-watch": "tsc -w",
    "serve-watch": "nodemon dist/server"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/jest": "^25.2.2",
    "@types/node": "^14.0.1",
    "@types/supertest": "^2.0.9",
    "jest": "^26.0.1",
    "nodemon": "^2.0.4",
    "npm-run-all": "^4.1.5",
    "supertest": "^4.0.2",
    "ts-jest": "^26.0.0",
    "typescript": "^3.9.2"
  }
}
